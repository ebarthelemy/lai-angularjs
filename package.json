{
  "name": "lai-angularjs",
  "private": true,
  "version": "0.0.0",
  "description": "A starter project for an AngularJS App using a Laravel API and implementing the Laravel out of the box authentication.",
  "repository": "https://github.com/ebarthelemy/my-project-angular",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8080 -c-1",
    "pretest": "npm install",
    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update"
  }
}